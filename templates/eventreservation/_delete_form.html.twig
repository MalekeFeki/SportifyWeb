<form method="post" action="{{ path('app_eventreservation_delete', {'reservationid': eventreservation.reservationid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventreservation.reservationid) }}">
    <button class="btn">Delete</button>
</form>
