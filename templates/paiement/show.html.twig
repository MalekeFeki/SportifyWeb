{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idp</th>
                <td>{{ paiement.idp }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ paiement.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ paiement.email }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ paiement.country }}</td>
            </tr>
            <tr>
                <th>Ncb</th>
                <td>{{ paiement.ncb }}</td>
            </tr>
            <tr>
                <th>Cvc</th>
                <td>{{ paiement.cvc }}</td>
            </tr>
            <tr>
                <th>Expdate</th>
                <td>{{ paiement.expdate ? paiement.expdate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Postalcode</th>
                <td>{{ paiement.postalcode }}</td>
            </tr>
            <tr>
                <th>Promocode</th>
                <td>{{ paiement.promocode }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ paiement.price }}</td>
            </tr>
            <tr>
                <th>Pdate</th>
                <td>{{ paiement.pdate ? paiement.pdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paiement_index') }}">back to list</a>

    <a href="{{ path('app_paiement_edit', {'idp': paiement.idp}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
