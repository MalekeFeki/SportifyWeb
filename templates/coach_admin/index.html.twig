{% extends 'base.html.twig' %}

{% block title %}Liste des commentaires{% endblock %}

{% block ajoutCoach %}
    <h1>Liste des commentaires</h1>
    <table border="1" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Description</th>
                <th>Sexe</th>
                <th>Photo</th> <!-- Add a new column for the photo -->
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for coach in coachs %}
            <tr>
                <td>{{ coach.id }}</td>
                <td>{{ coach.nomCo }}</td>
                <td>{{ coach.prenomCo }}</td>
                <td>{{ coach.description }}</td>
                <td>{{ coach.sexe }}</td>
                <td><img src="{{ asset('path_to_your_photos_directory/' ~ coach.photo) }}" alt="Coach Photo" style="width: 100px;"></td> <!-- Display the photo -->
                <td>
                    <a href="{{ path('app_coach_admin_edit', {'id': coach.id}) }}"><button>modifier</button></a>
                    <form method="post" action="{{ path('app_coach_admin_delete', {'id': coach.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.id) }}">
                        <button>supprimer</button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Aucun utilisateur trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_coach_admin_new') }}">Create new</a>
{% endblock %}
