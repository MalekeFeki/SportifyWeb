{% extends 'base.html.twig' %}

{% block title %}Challenge{% endblock %}

{% block body %}
    <h1>Challenge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idc</th>
                <td>{{ challenge.idc }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ challenge.nom }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ challenge.difficulty }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ challenge.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_challenge_index') }}">back to list</a>

    <a href="{{ path('app_challenge_edit', {'idc': challenge.idc}) }}">edit</a>

    {{ include('challenge/_delete_form.html.twig') }}
{% endblock %}
