<form method="post" action="{{ path('app_salle_delete', {'idS': salle.idS}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.idS) }}">
    <button class="btn btn-danger">Delete</button>
</form>
