{% extends 'base.html.twig' %}

{% block title %}Edit Salle{% endblock %}

{% block body %}
    <h1>Edit Salle</h1>

    {{ include('salle/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row">
        <div class="col">
            <a href="{{ path('index') }}" class="btn btn-secondary mr-2">Back to List</a>
            <a href="{{ path('app_seance_new', {'idS': salle.idS}) }}" class="btn btn-primary mr-2">Add Seance</a>
            {{ include('salle/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
