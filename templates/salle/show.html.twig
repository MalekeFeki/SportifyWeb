{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ids</th>
                <td>{{ salle.ids }}</td>
            </tr>
            <tr>
                <th>Noms</th>
                <td>{{ salle.noms }}</td>
            </tr>
            <tr>
                <th>Addressed</th>
                <td>{{ salle.addressed }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ salle.region }}</td>
            </tr>
            <tr>
                <th>Options</th>
                <td>{{ salle.options }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'ids': salle.ids}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
